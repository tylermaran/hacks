{
	"name": "lenny",
	"version": "1.0.0",
	"description": "Lenny is a multipart project to play around with knowledge graphs and neural nets.",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"type": "module",
	"dependencies": {
		"dotenv": "^16.0.1",
		"express": "^4.18.1"
	}
}
