const blocks = require('./blocks');

const generateBlocks = () => {
	console.log(blocks);

	blocks.forEach((element) => {});

	const block = `<div></div>`;

	for (let i = 0; i < 9; i++) {
		const element = array[i];
	}
};

generateBlocks();
